"use client";

const pipelineSteps = [
  { label: "Requirements", icon: "📋" },
  { label: "System Design", icon: "🏗️" },
  { label: "Development", icon: "💻" },
  { label: "Testing", icon: "🧪" },
  { label: "Deployment", icon: "🚀" },
  { label: "Monitoring", icon: "📡" },
];

export default function ProjectHighlight() {
  return (
    <section id="project-highlight" style={{ position: "relative", zIndex: 1 }}>
      <div className="section-container">
        <h2 className="section-title">Project Highlight</h2>

        <div
          style={{
            display: "grid",
            gridTemplateColumns: "1fr 1.2fr",
            gap: 40,
            alignItems: "center",
          }}
          className="highlight-grid"
        >
          {/* Left — Project Card */}
          <div
            style={{
              background: "rgba(7, 20, 40, 0.9)",
              border: "1px solid rgba(59, 130, 246, 0.25)",
              borderRadius: 20,
              padding: "28px",
              position: "relative",
              overflow: "hidden",
            }}
          >
            {/* Top glow line */}
            <div
              style={{
                position: "absolute",
                top: 0,
                left: 0,
                right: 0,
                height: 2,
                background:
                  "linear-gradient(90deg, transparent, #3b82f6, #22d3ee, transparent)",
              }}
            />

            {/* Project header */}
            <div
              style={{
                display: "flex",
                alignItems: "center",
                gap: 10,
                marginBottom: 20,
              }}
            >
              <div
                style={{
                  width: 36,
                  height: 36,
                  borderRadius: 10,
                  background: "rgba(59,130,246,0.15)",
                  border: "1px solid rgba(59,130,246,0.3)",
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  fontSize: "1rem",
                }}
              >
                🤖
              </div>
              <div>
                <h3
                  style={{
                    fontFamily: "var(--font-space)",
                    fontSize: "0.95rem",
                    fontWeight: 700,
                    color: "#f0f6ff",
                  }}
                >
                  AI Assistant System (ASMI)
                </h3>
              </div>
            </div>

            {/* Problem / Solution */}
            <div style={{ display: "flex", flexDirection: "column", gap: 12, marginBottom: 20 }}>
              <div style={{ display: "flex", gap: 8 }}>
                <span style={{ color: "#f0f6ff", fontWeight: 700, fontSize: "0.85rem", minWidth: 80 }}>
                  Problem:
                </span>
                <span style={{ color: "#94a3b8", fontSize: "0.85rem", lineHeight: 1.5 }}>
                  Difficulty in accessing company knowledge
                </span>
              </div>
              <div style={{ display: "flex", gap: 8 }}>
                <span style={{ color: "#f0f6ff", fontWeight: 700, fontSize: "0.85rem", minWidth: 80 }}>
                  Solution:
                </span>
                <span style={{ color: "#94a3b8", fontSize: "0.85rem", lineHeight: 1.5 }}>
                  Built an AI chatbot with knowledge base
                </span>
              </div>
            </div>

            {/* Architecture mini diagram */}
            <div
              style={{
                background: "rgba(4, 16, 32, 0.8)",
                border: "1px solid rgba(59,130,246,0.15)",
                borderRadius: 12,
                padding: "12px 16px",
                marginBottom: 16,
              }}
            >
              <span style={{ fontSize: "0.75rem", color: "#475569", fontWeight: 600, display: "block", marginBottom: 8 }}>
                Architecture
              </span>
              <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap" }}>
                {["☁️ Cloud", "⚡ Vector DB", "🧠 LLM", "⚙️ API", "📱 Client"].map((item, i) => (
                  <div key={item} style={{ display: "flex", alignItems: "center", gap: 6 }}>
                    <span style={{ fontSize: "0.75rem", color: "#60a5fa", fontWeight: 500 }}>{item}</span>
                    {i < 4 && (
                      <svg width="14" height="10" viewBox="0 0 14 10" fill="none">
                        <path d="M0 5h12M8 1l4 4-4 4" stroke="rgba(59,130,246,0.4)" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/>
                      </svg>
                    )}
                  </div>
                ))}
              </div>
            </div>

            {/* Tech Stack */}
            <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 16, flexWrap: "wrap" }}>
              <span style={{ fontSize: "0.8rem", color: "#475569", fontWeight: 600 }}>Tech Stack:</span>
              {["React", "Node.js", "LLM"].map((tech) => (
                <span key={tech} className="tech-badge">{tech}</span>
              ))}
            </div>

            {/* Impact */}
            <div
              style={{
                display: "flex",
                alignItems: "center",
                gap: 10,
                padding: "10px 14px",
                background: "rgba(34, 197, 94, 0.08)",
                border: "1px solid rgba(34, 197, 94, 0.2)",
                borderRadius: 10,
                marginBottom: 20,
              }}
            >
              <div
                style={{
                  width: 20,
                  height: 20,
                  borderRadius: "50%",
                  background: "rgba(34, 197, 94, 0.2)",
                  border: "1px solid rgba(34, 197, 94, 0.4)",
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  fontSize: "0.7rem",
                  flexShrink: 0,
                }}
              >
                ✓
              </div>
              <span style={{ fontSize: "0.83rem", color: "#94a3b8" }}>
                Impact:{" "}
                <span className="impact-highlight">70% Reduction</span>{" "}
                in Manual Queries
              </span>
            </div>

            <a href="/projects" className="btn-primary" style={{ fontSize: "0.82rem", padding: "8px 18px" }}>
              View Details
            </a>
          </div>

          {/* Right — Pipeline */}
          <div>
            <p style={{ color: "#475569", fontSize: "0.82rem", fontWeight: 600, marginBottom: 24, textTransform: "uppercase", letterSpacing: "0.08em" }}>
              End-to-End Delivery Pipeline
            </p>

            {/* Vertical pipeline on mobile, horizontal grid on desktop */}
            <div
              style={{
                display: "grid",
                gridTemplateColumns: "repeat(3, 1fr)",
                gap: 16,
              }}
              className="pipeline-grid"
            >
              {pipelineSteps.map((step, index) => (
                <div
                  key={step.label}
                  style={{
                    display: "flex",
                    flexDirection: "column",
                    alignItems: "center",
                    gap: 10,
                    position: "relative",
                  }}
                >
                  {/* Step card */}
                  <div
                    className="pipeline-card"
                    style={{
                      width: "100%",
                      background: "rgba(7, 20, 40, 0.8)",
                      border: "1px solid rgba(59, 130, 246, 0.2)",
                      borderRadius: 14,
                      padding: "16px 12px",
                      display: "flex",
                      flexDirection: "column",
                      alignItems: "center",
                      gap: 8,
                      transition: "all 0.3s ease",
                      cursor: "default",
                    }}
                  >
                    {/* Step number */}
                    <div
                      style={{
                        width: 24,
                        height: 24,
                        borderRadius: "50%",
                        background: "rgba(59,130,246,0.2)",
                        border: "1px solid rgba(59,130,246,0.4)",
                        display: "flex",
                        alignItems: "center",
                        justifyContent: "center",
                        fontSize: "0.65rem",
                        fontWeight: 700,
                        color: "#3b82f6",
                      }}
                    >
                      {index + 1}
                    </div>
                    <span style={{ fontSize: "1.4rem" }}>{step.icon}</span>
                    <span
                      style={{
                        fontSize: "0.72rem",
                        color: "#94a3b8",
                        fontWeight: 600,
                        textAlign: "center",
                        lineHeight: 1.3,
                      }}
                    >
                      {step.label}
                    </span>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>

      <style>{`
        @media (max-width: 768px) {
          .highlight-grid {
            grid-template-columns: 1fr !important;
          }
          .pipeline-grid {
            grid-template-columns: repeat(2, 1fr) !important;
          }
        }
      `}</style>
    </section>
  );
}
